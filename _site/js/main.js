function contactFunction() {
    $('html,body').animate({
        scrollTop: $(".wrapper").offset().top},
        'slow');
};

function topPage() {
    $('html,body').animate({
        scrollTop: $("#am").offset().top},
        'slow');
};
